#include<iostream>
using namespace std;
int main()
{//substr() = cut
 //insert(index, str) = chen
 //erase(index, numb) = xoa 
 //compare(index,) = so sanh
 //find(str, index = tim kiem
	string str1 ("green apple");
	string str2 ("red apple");
	if (str1.compare(str2) !=0)
		cout<< "Khac";
	else{
		cout<<"Giong";
	}
	return 0 ;
}
